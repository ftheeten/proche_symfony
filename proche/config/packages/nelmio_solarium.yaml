nelmio_solarium:
    endpoints:
        default:
            scheme: http
            host: localhost
            port: 8983
            path: /
            core: proche-prod
        proche_constituents:
            scheme: http
            host: localhost
            port: 8983
            path: /
            core: proche-constituents
    clients:
        default:
            endpoints: [default, proche_constituents]
